<?xml version="1.0" encoding="UTF-8"?>
<!--
  C3 可观测性：
   - prod：结构化 JSON（logstash-logback-encoder，含 MDC traceId + 静态 app 字段），可被日志平台（ELK/Loki 等）解析；
   - 非 prod（dev/test）：人类可读 pattern（含 traceId），便于本地调试。
  日志级别仍由 application.yml 的 logging.level.* 控制（com.blms: info）。
-->
<configuration>
  <springProfile name="prod">
    <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
      <encoder class="net.logstash.logback.encoder.LogstashEncoder">
        <customFields>{"app":"bulkhaul-server"}</customFields>
      </encoder>
    </appender>
    <root level="INFO">
      <appender-ref ref="JSON"/>
    </root>
  </springProfile>

  <springProfile name="!prod">
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
      <encoder>
        <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] [%X{traceId:-}] %logger{36} - %msg%n</pattern>
      </encoder>
    </appender>
    <root level="INFO">
      <appender-ref ref="CONSOLE"/>
    </root>
  </springProfile>
</configuration>
