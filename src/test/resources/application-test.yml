spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/blms_test?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: blms
    password: blms123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

blms:
  scheduler:
    auto-enabled: false

logging:
  level:
    com.blms: warn
    org.apache.ibatis: warn
